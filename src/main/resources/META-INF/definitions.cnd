<jmix = 'http://www.jahia.org/jahia/mix/1.0'>
<jnt = 'http://www.jahia.org/jahia/nt/1.0'>
<crh = 'http://www.jahia.org/crh/1.0'>

// Opt-in marker. Only nodes carrying this mixin are snapshotted on publication.
//
// A PAGE or a CONTENT node. Content that is published and visible without a page of its own -- a
// policy block reused across pages, a component with its own lifecycle -- needs a revision history
// just as much, and nothing in the storage was ever page-shaped: the folder is keyed on the marked
// node's UUID. What had to change alongside this was the three separate walks that answered "which
// node owns this history", two of which walked to the nearest jnt:page. See RevisionedAncestor.
[jmix:publiclyRevisioned] mixin
 extends = jnt:page, jnt:content

// Public, editorial container rendered on the page. Holds only revision entries.
[crh:revisionHistory] > jnt:content, jmix:droppableContent, jmix:editorialContent, jmix:list
 // `orderable` is REQUIRED and is not implied by jmix:list -- jnt:contentList declares it
 // separately for exactly this reason. Without it Jackrabbit refuses reordering
 // ("child node ordering not supported on node ..."), so editors cannot drag entries into
 // order at all. Both the list view and the comparison treat the next sibling in editorial
 // order as the older revision, so a non-orderable history left that convention
 // unachievable: the order was whatever insertion order the repository happened to give,
 // and "compare with previous" could name a pair the editor never chose.
 orderable
 itemtype = content
 - historyTitle (string) i18n
 // Whether the list starts closed. Defaults to true: a revision history is supporting
 // evidence for the page, not the page, and a policy that has been revised twenty times
 // otherwise pushes its own content off the screen. The entries stay in the DOM either way --
 // <details> hides them from view, not from search engines, find-in-page or assistive
 // technology -- so nothing about the public record depends on this being open.
 - collapsedByDefault (boolean) = true
 + * (crh:revisionEntry)

// One public revision. Authored by an editor; publishing it is what makes it public.
[crh:revisionEntry] > jnt:content, jmix:droppableContent, jmix:editorialContent
 itemtype = content
 - revisionLabel (string) mandatory
 - revisionDate (date) mandatory
 - summary (string, richtext) i18n mandatory
 - changeType (string, choicelist[resourceBundle]) = 'substantive' < 'editorial', 'substantive', 'correction'
 // Optional. Empty means "bind to whatever snapshot is current when this entry is published",
 // which is right for the normal rhythm of one publication producing one entry and one snapshot.
 // Backfill breaks that pairing -- many snapshots already exist and the entries describing them
 // are written afterwards -- so an editor can pin the one an entry actually describes. The
 // choicelist is served by SnapshotChoiceListInitializer, which registers itself under this key.
 // SUPERSEDED by crh:pinnedSnapshot below, and deliberately left exactly as it is.
 //
 // The pin has to be per language: it names a snapshot inside the PER-LANGUAGE folder
 // revision-history/<pageUuid>/<lang>/, and those names embed a per-language content hash, so a
 // name valid in /en never resolves in /fr. One shared value could not express that -- pinning an
 // entry while editing in English left the French comparison reporting "no snapshot recorded"
 // permanently, a state the model could not represent rather than a bug in the resolver.
 //
 // Adding `i18n` here is NOT the fix, and it is not a matter of taste: Jahia's
 // DefinitionsBundleChecker classes a change of this flag on an existing property as a MAJOR
 // definition change and CANCELS THE WHOLE MODULE DEPLOYMENT. Measured on 8.2.4.0, not assumed:
 //   ERROR [DefinitionsBundleChecker] - Major changes detected:
 //   [nodeTypeName=crh:revisionEntry,type=MAJOR,
 //    propDefDiffs={[itemName=crh:snapshotRef,type=MAJOR,operation=MODIFIED]}]
 // So an existing property cannot be converted in place; a new one has to be added beside it and
 // the old one kept readable. Anything already pinned through this property keeps working --
 // RevisionEntryBinder reads it as a fallback -- so nothing an editor set is lost.
 - crh:snapshotRef (string, choicelist[crhSnapshots])
 // The per-language pin. Same choicelist, because the picker already offers only the snapshots of
 // the page and language being edited; it is the STORAGE that had to become per language.
 - crh:pinnedSnapshot (string, choicelist[crhSnapshots]) i18n
 // NOTE: the property above is the EXPLICIT PIN, and it is not how an entry is normally bound.
 // Automatic binding stores the link on the SNAPSHOT
 // (crh:revisionSnapshot / crh:entryRefs), pointing back at the entry, for one decisive
 // reason: a crh:revisionEntry is editorial content, so any programmatic write to it bumps
 // jcr:lastModified and the page reads as "modified" in jContent the instant capture binds
 // it -- immediately after the editor published it. That turns every new revision into a
 // two-publication ritual and a support question. A snapshot, by contrast, is system-only and
 // never published, so binding there is invisible to the editorial workflow.
 // The lookup entry -> snapshot is a scan of that page+language folder, which the
 // MAX_SNAPSHOTS_PER_PAGE_LANGUAGE cap bounds at 500 nodes, and it runs only when a visitor
 // actually asks for a comparison.

// Container for the snapshot tree, at all three levels
// (revision-history / <pageUuid> / <language>).
// jmix:droppableContent is a *structural* requirement, not an editorial affordance:
// /sites/<site>/contents is a jnt:contentFolder whose only wildcard child definitions are
// `+ * (jmix:droppableContent)` and `+ * (jnt:contentFolder)`, so without it the root folder
// cannot be created at all (verified: ConstraintViolationException "No child node definition
// for revision-history").
// crh:revisionSnapshot does NOT carry it, and the filter named above is not the one that decides
// whether a snapshot is visible. That belief shipped here for two releases and this comment was
// the last place still holding it: includeTypes [jmix:droppableContent, jnt:page, jnt:file] is the
// content PICKER's filter. The content BROWSER filters flat views on
// [jmix:editorialContent, jmix:queryContent] and recurses the tree only into
// [jnt:page, jnt:contentFolder, jnt:folder, ...]. That is why crh:revisionSnapshot carries
// jmix:editorialContent (so the flat view lists it) and the folder is a contentFolder type (so the
// tree can recurse into it). 1.3.0 verified only the FOLDER, against the wrong filter, which left
// 28 snapshots present and 0 shown -- folders you can open onto an empty list.
// Pinned by tests/cypress/e2e/07-jcontentUi.cy.ts, which drives jContent in a browser precisely
// because a GraphQL assertion proves only what the author believed the filter was. Neither type appears as a component an
// editor can add: this module generates no components node at all (verified across every
// deployed version), and the child definitions below are explicit, so nothing may be created
// inside a snapshot folder except what capture puts there.
// These types used to carry jmix:hiddenType as well. That was dropped so the store can be
// browsed and previewed in jContent, which is what makes it possible to describe backfilled
// history: an editor has to be able to read a snapshot before writing the revision entry for
// it. jmix:hiddenType is not merely "keep it out of the components list" -- jContent's browse
// query lists it in excludeTypes, so it hid the whole tree from the content browser too.
// The tree inherits the site's content permissions: it used to have ACL inheritance broken with
// nothing granted, which left it readable only by root -- not by administrators, whose privileges
// are themselves delivered through ACL entries -- and that made the snapshot picker useless.
// What still holds is jmix:nolive, which keeps the tree out of the live workspace entirely. Extending jnt:contentFolder
// instead of carrying jmix:droppableContent was measured as an alternative and does NOT work:
// jContent's browse filter is includeTypes [jmix:droppableContent, jnt:page, jnt:file], so a
// folder that is not droppable is invisible again.
// The crh:latest* / crh:snapshotCount properties are a denormalised index on the language
// folder: dedupe used to scan every historical snapshot node -- each carrying a full page's
// Markdown -- on every capture, so its cost grew linearly and forever.
// The crh:lastCapture* properties make a missing snapshot self-explaining: without them a gap
// in the record is indistinguishable from "the content did not change", which is the single
// distinction this feature exists to make.
[crh:snapshotFolder] > jnt:contentFolder, jmix:nolive
 - crh:latestHash (string) indexed=no nofulltext hidden
 - crh:latestSnapshot (string) indexed=no nofulltext hidden
 - crh:snapshotCount (long) indexed=no hidden
 - crh:prunedCount (long) indexed=no hidden
 - crh:lastCaptureStatus (string) indexed=no nofulltext hidden
 - crh:lastCaptureMessage (string) indexed=no nofulltext hidden
 - crh:lastCaptureDate (date) indexed=no hidden
 + * (crh:snapshotFolder)
 + * (crh:revisionSnapshot)

// System-generated, never published, never editor-edited. Append-only record.
// indexed=no / nofulltext throughout: historical copies must not pollute site search.
// crh:capturedBy records the principal the render ran as: `guest` unless a deployment configures
// a capture user, which exists so that pages the public cannot read can have a history at all.
// This comment used to say "always guest" and use that to argue a snapshot is safe to show
// publicly. That argument expired when capture became configurable, and it was never the thing
// keeping the read path safe: RevisionDiffService#viewerMayReadHistory is, by checking the
// CURRENT user's own JCR rights before any snapshot is read. Read crh:capturedBy as provenance --
// whose view of the page this text represents, and therefore who may safely be shown it.
// crh:markdown is binary, not string, so reading a snapshot's metadata does not drag the whole
// page payload along with it.
// jmix:nolive is what keeps this tree out of the live workspace, and it is a stronger
// statement than it looks: JCRPublicationService honours it directly, and the platform applies
// it to types that must never exist in live at all (jnt:role, jnt:permission, jnt:component,
// jnt:configurationFile). Publishing an ancestor -- /sites/<site>/contents, say -- therefore
// cannot drag snapshots into live along with it.
//
// Work-in-progress (j:workInProgressStatus = ALL_CONTENT) would also skip publication, and was
// considered. It is the wrong tool twice over: it is an EDITORIAL signal meaning "I have not
// finished this yet", shown as a badge in jContent, and any editor can clear it on any node --
// so it states something untrue about an immutable record and does not hold. jmix:nolive is
// declared on the TYPE, so there is no per-node flag to clear.
//
// The editorial types (crh:revisionHistory, crh:revisionEntry) deliberately do NOT carry it:
// they are the public half and must publish normally.
// Every property below is `hidden`: written by capture, never typed. The edit engine offered all
// of them as editable fields -- measured with forms.editForm, crh:markdown came out as
// requiredType BINARY with selectorType Text and readOnly false, i.e. a single-line input for a
// document that may be a megabyte, sitting next to an editable crh:contentHash. An editor could
// retype the record and its own hash. `hidden` is the platform's own answer for a property that
// code sets and users must not (j:published, j:workInProgress use it); `protected` is NOT used
// here, because that would refuse this module's own writes.
//
// Nothing is lost by hiding them. Views read properties directly, so the snapshot preview still
// shows the metadata and the Markdown as preformatted text -- that is what it exists for.
// crh:snapshotRef on crh:revisionEntry stays visible: it is the one field an editor is meant to set.
//
// A textarea was considered and is the wrong fix. BINARY gets selectorType Text from the platform
// whatever the label, so a real textarea would mean (string, textarea) -- which would drag up to a
// megabyte into every metadata read (the snapshot picker deliberately reads a bounded 512-byte
// prefix), move the payload from the datastore into the workspace bundle, and invalidate every
// existing binary value.
[crh:revisionSnapshot] > jnt:content, jmix:editorialContent, jmix:nolive
 - crh:snapshotDate (date) mandatory indexed=no hidden
 - crh:language (string) mandatory indexed=no nofulltext hidden
 - crh:contentHash (string) mandatory indexed=no nofulltext hidden
 - crh:generatorVersion (string) mandatory indexed=no nofulltext hidden
 - crh:capturedBy (string) mandatory indexed=no nofulltext hidden
 - crh:sourceUrl (string) indexed=no nofulltext hidden
 - crh:markdown (binary) mandatory indexed=no nofulltext hidden
 // Back-references to the crh:revisionEntry nodes this snapshot is the content for. Set by
 // RevisionEntryBinder after each capture attempt; see the note on crh:revisionEntry for why
 // the reference points this way. Multiple, because an editor can author more than one entry
 // between two captures. WEAK references: an entry may be deleted, and a snapshot must never
 // block that or be dragged into referential-integrity failures -- the record of what the
 // page said outlives the editorial description of why it changed.
 - crh:entryRefs (weakreference) multiple indexed=no nofulltext hidden
